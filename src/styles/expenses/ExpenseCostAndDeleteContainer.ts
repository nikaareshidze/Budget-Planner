import styled from "styled-components";

const ExpenseCostAndDeleteContainer = styled.div`
  display: flex;
  align-items: center;
  gap: 1em;
  background-color: white;
`;

export default ExpenseCostAndDeleteContainer;
